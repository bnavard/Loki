# X-Portrait pip deps. Torch 2.0.1+cu118 comes from env.yml; this file
# handles everything else. Mirrors upstream's `impl/requirements.txt` with
# a handful of small tweaks documented inline.

# ---------------------------------------------------------------------------
# Core deep-learning stack (pinned — tied to torch 2.0.x)
# ---------------------------------------------------------------------------
diffusers==0.26.0
# diffusers 0.26 still imports `cached_download` from huggingface_hub, which
# was removed in huggingface_hub 0.26. Pin to the last 0.25.x release.
huggingface_hub==0.25.2
transformers==4.30.0
tokenizers==0.12.1
xformers==0.0.22
triton==2.0.0
pytorch-lightning
torchdata==0.6.1
torchmetrics==0.6.0
accelerate==0.17.0
ema-pytorch==0.2.1

# CLIP (from GitHub, as upstream specifies)
clip @ git+https://github.com/openai/CLIP.git
open-clip-torch>=2.20.0

# ---------------------------------------------------------------------------
# Numeric / image / video I/O
# ---------------------------------------------------------------------------
numpy==1.23.0
scipy>=1.10.1
scikit-image==0.19.3
opencv-python==4.7.0.72
imageio==2.9.0
imageio-ffmpeg==0.4.2
kornia==0.6.11
decord
face-alignment

# ---------------------------------------------------------------------------
# Config / training utils
# ---------------------------------------------------------------------------
einops>=0.6.1
einops_exts==0.0.4
rotary_embedding_torch==0.2.1
entmax==1.1
torchdiffeq==0.2.3
omegaconf>=2.3.0
fire>=0.5.0
fsspec>=2023.6.0
pyyaml>=5.4.1
tqdm>=4.65.0
timm>=0.9.2
matplotlib>=3.7.2
pillow
pandas>=2.0.3
natsort>=8.4.0
ninja>=1.11.1
regex==2022.10.31
ftfy==6.1.1
chardet==5.1.0
urllib3<1.27,>=1.25.4
tensorboard==2.11.2
tensorboardx==2.6
wandb>=0.15.6
wheel>=0.41.0
fairscale>=0.4.13
invisible-watermark>=0.2.0
webdataset>=0.2.33

# Upstream lists but X-Portrait's test path doesn't use them at runtime.
# Kept for upstream-install parity; can be trimmed if the env is too fat.
streamlit>=0.73.1
streamlit-keyup==0.2.0
pudb>=2022.1.3
thriftpy2

# ---------------------------------------------------------------------------
# Added by us
# ---------------------------------------------------------------------------
# Google Drive downloader — upstream hosts the checkpoint on GDrive; the
# direct-download URL tokens are ephemeral, so we use gdown's id-based API
# in setup_env.sh for stable behaviour.
gdown

# SadTalker pins — Pouyan's verified working set on H200 (sm_90) with the
# torch 2.1.0+cu121 stack installed by env.yml. These are derived from a
# known-good `pip list` and differ from upstream's requirements.txt (which
# still targets torch 1.12.1 + numpy 1.21.6). Use this file instead.

# Core scientific stack
numpy==1.23.4
scipy==1.10.1
pandas==2.0.3
numba==0.58.1
llvmlite==0.41.1
scikit-learn==1.1.3
scikit-image==0.19.3
matplotlib==3.7.5

# Image / video I/O
imageio==2.19.3
imageio-ffmpeg==0.4.7
opencv-python==4.13.0.92
Pillow==10.4.0
PyWavelets==1.4.1
tifffile==2023.7.10
av==12.3.0

# Audio stack — librosa is pinned below its 0.10 line on purpose; SadTalker's
# audio preprocessing calls deprecated APIs that 0.10 renamed / removed.
librosa==0.9.2
audioread==3.0.1
resampy==0.3.1
soundfile==0.13.1
pydub==0.25.1
soxr; platform_system != "Windows"

# Face detection / restoration / enhancer stack
face-alignment==1.3.5
facexlib==0.3.0
basicsr==1.4.2
gfpgan==1.3.8
filterpy==1.4.5
kornia==0.6.8

# Model / config / serialization
safetensors==0.5.3
yacs==0.1.8
PyYAML==6.0.3
addict==2.4.0

# Utilities
tqdm==4.67.3
joblib==1.1.0
future==1.0.0
yapf==0.43.0

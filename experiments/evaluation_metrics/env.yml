name: evaluation_metrics
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pytorch=2.1.*
  - torchvision
  - pytorch-cuda=11.8
  - ffmpeg                  # decord + imageio backend
  - pip:
      - -r requirements.txt

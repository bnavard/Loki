# Pinned for reproducibility — bump with care.

insightface>=0.7.3         # buffalo_l: RetinaFace + ArcFace R100
onnxruntime-gpu==1.16.3    # last release with CUDA 11.8 + cuDNN 8 support
decord                     # fast random-access video decode
imageio[ffmpeg]            # decode fallback when decord isn't available
scipy                      # quaternion math for head_rot's geodesic distance
numpy<2                    # insightface ext modules are still compiled against
                           # numpy 1.x. Lift this when the upstream stack
                           # catches up.
tqdm
pytest                     # tests/
